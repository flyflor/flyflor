这个项目是 llm 模型作为**流体智力** + MemoryComponent(**热记忆**，人类的当前记忆缓存，融合 openhuman 知识树向量召回) + Scope(用户明确创建项目/开启的新事情 将触发 ASK 确定后**固化**，或者是用户频繁提起的项目/事情/等，会形成codename候选，一定条件下升格为 Scope) + CrystalComponent(向量晶体智力) 构成的无 session 智能生命体。

晶体智力：llm 处理问题遇到无法决策的时候直接向用户抛回 1-n 点 ASK，每个 ASK 有 1-3 个选项(第一个为有推荐方案)，这里要注意提示词工程。用户选择后事件继续，形成结晶候选。这个候选命中一定次数后会升格为晶体智力 Gem，类似于 SKILL 的一种做事方式，由 LLM 自我总结。

Scope: 具有独立的宪法层，在 .config 中有固化的项目目录和单独的 scope.db，这里的 scope.db 是类似于 MemoryComponent 的热记忆。但它具有 Vector 向量索引，当用户提起 Scope 关键词，智能体会陷入回忆中。通过向量范围动态装载热记忆，即 "回忆中"。

路由: 在线索收集摘要、理解意图、出现困惑升格启动黑板讨论(这里默认是 llm 自动分配多角色讨论，需要留个配置后续黑板可能是 opencode 讨论者是 codex claude kimi 等等)。如果触发封顶(3 轮开始收敛 5 轮硬封顶)会抛出 ASK。每次 ASK 都会有结晶候选 和 EQ 机制。当结晶候选到一定程度，将成为晶体智力，到我们的 CrystalComponent 向量数据封装中。

遗忘机制: 遗忘不是删除，热记忆会被压缩，晶体记忆会发生偏移。这都由 llm 模型完成，类似于人类的记忆系统。这个机制我们设计为按时间自动模糊和偏移，参考人类的以往曲线。

brain.db: 全量记录用户与智能体的所有交互，每个月会产生一个新的 db 文件。TUI 的历史记录，fork，思考，黑板等，都靠这个全量记忆。记忆是由线索收集、理解意图构成的，并添加所有历史线索的知识树 + Scope 回忆层构成的知识树，参考 openhuman。不同的是我们的全文知识树会随遗忘曲线模糊，但是这个模糊的非常慢，我们需要设计一个合理的机制。而且由于我们有生命账本，所以全文知识树有重新摘要、理解意图、回忆机制。总得来说我们的记忆树由两部分构成 Scope + 全文记忆树，暴露在上下文的是意图理解和摘要告诉模型用户想做什么，它该做什么。

同时这个智能体具有 hermes-agent 的执行力，主要参考 nanobot 简洁的工具 scope 划分，hermes-agent调度模式。当调度额度用完触发ask闭环结晶和完成超长线loop任务，这类似于openclaw的超长线loop能力。

上下文摘要探索能力以 codex 为基础扩展改造，融合 claude code 多点编辑和摘要能力，增强 opencode 子代理并发能力。让对话更流畅，理解用户意图，精准召回。

@Plugin 外挂式工具，如 rtk、codegraph、browser use、computer use 等，@Tool 是内置工具如 bash 等。

@Sandbox 沙盒守卫，以 RxJS 为基础做侦查者守卫。判断 Confirm 请求，这里注意一下 Confirm 不属于 ASK，ASK 是更高等级的存在。这是一个会问问题的智能生命体，它会结晶，会 Scope 固化的宪法层回忆。有完美的红线和独立记忆，但拥有向量召回能力，这类似于openhuman记忆关联树。

# 上述提到的所有项目参考

地址: /Users/yihuaqing/Desktop/yihuaqing/flyflors/reference
