import { Module } from "../di";

/**
 * Assembles repository classes for data model and SQL operations.
 *
 * @usage Add `@Repo()` providers here when persistence entities are introduced.
 */
@Module({})
export class EntitiesModule {}
