/**
 * Compatibility re-export for legacy imports.
 *
 * New code should import shared component bases from `src/components/index.ts`
 * so the component layer stays visibly separate from the agent boundary.
 */
export * from "../components/index.ts";
