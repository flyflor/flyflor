/** Newline-delimited JSON frame separator used by the IPC wire protocol. */
export const PACKET_FRAME_SEPARATOR = '\n';

/** Text encoding used by the IPC socket stream. */
export const PACKET_TEXT_ENCODING = 'utf-8';
