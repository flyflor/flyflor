# Project Report

## Current State

Flyflor is aligned around the Bun-kernel Cognitive-Executive-Agent Architecture.

The active runtime surface is:

- Bun source and compiled Bun binary.
- Local chat debug entry.
- Socket vascular surface: `/ws` and `/health`.
- Event/control/read-model snapshots through `/ws`.

The active cognition contract is:

- Mindstream as fluid intelligence.
- Memory as hot memory and recall equipment.
- Crystal as crystallized intelligence.
- Scope as explicit durable work domain.
- ContextFork as explicit branch.
- ASK as the closure organ for uncertainty and long-horizon loops.
- Executive as the auditable action exoskeleton.

## Sealed Boundaries

- `brain.db` is ledger/query/replay/audit/detail only.
- Prompt assembly uses Constitution, Crystal, Memory, Scope/Fork, Executive visible capabilities and request context.
- Transport metadata does not own cognitive continuity.
- `gateway.*` remains wire compatibility only.
- HTTP surface remains `/ws` and `/health`.
- Rust-shell material is archived as future external-repo handoff.

## Documentation Refresh

On 2026-05-25 the stale active documentation was archived into `docs/old-docs/2026-05-25-docs-refresh/` and regenerated from the current source layout. The new docs keep English/Chinese structure synchronized and focus on the Bun kernel/gateway workspace.
