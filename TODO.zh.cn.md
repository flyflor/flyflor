# TODO

## 2026-06-02

- [ ] 接入真实模型 provider，并建立 fast/thinking 场景测试。
- [ ] 为 `CapillaryModule.consult()` 接入真实 Confirm、ASK 和 sandbox 订阅者。
- [ ] 为 `src/socket` 增加完整 WebSocket live turn/control/event 协议场景。
- [ ] 为 `src/entities` 接入真实 SQLite 或 Bun SQL 存储后端。
