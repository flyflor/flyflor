# Abandon Boundary

本目录只用于废弃代码备份。

它不是兼容层，不是主线运行时依赖，也不允许被主线 import / re-export。

## 当前内容

- 旧第一方 command / CLI / TUI 代码备份
- 旧第一方 gateway channel adapter 代码备份
- 旧第一方 gateway body 代码备份

## 规则

- 只备份，不回流
- 只归档，不扩张
- 主线协议与运行时边界以 `src/`、`docs/`、`TODO.md` 为准
