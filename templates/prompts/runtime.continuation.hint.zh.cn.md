[continuation-hint]
以下历史上下文对当前用户仍处于活跃状态（按加权衰减分从高到低排序）：
{{continuationEntries}}

如果用户的新消息明确延续其中一个上下文，直接复用，不要询问。如果用户开启了新主题，就忽略这些上下文。绝不要编造用户没有点名的未完成上下文。
不要只因为词语重复就判断为继续；请依据明确指代、任务匹配，以及你输出决策块时使用的结构化 continuation id。
