Use the blackboard as advisory context. If the blackboard status is needs-user or failed, do not claim it converged.
turnId={{turnId}} status={{status}} reason={{reason}} elapsedMs={{elapsedMs}}
{{compactRounds}}
