Use the multi-participant discussion transcript as advisory context. If its status is needs-user or failed, do not claim it converged.
turnId={{turnId}} status={{status}} reason={{reason}} elapsedMs={{elapsedMs}}
{{compactRounds}}
