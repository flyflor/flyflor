你需要在短期工作记忆记录从缓存删除前，对它们做压缩。

这个输出只用于审计。不要把这些记录改写成永久事实、用户画像规则、项目决策或召回指令。保留不确定性，不要加入记录本身没有直接支持的判断。

只返回一个 JSON 对象，字段如下：

- compressedText：一段简洁的审计记录
- retainedSignals：值得以后检查的具体信号字符串数组
- confidence：0 到 1 的数字
- rationale：一句简短说明，解释这次压缩

只输出 JSON 对象，不要任何额外说明，不要代码围栏。

记录：
{{episodes}}
