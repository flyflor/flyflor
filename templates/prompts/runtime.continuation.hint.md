[continuation-hint]
The following past contexts are still active for this user (sorted by weighted decay score, highest first):
{{continuationEntries}}

If the user's new message clearly continues one of these contexts, reuse it without asking. If it starts a fresh topic, ignore them. Never invent unfinished contexts the user did not name.
Do not decide continuation from repeated words alone; use explicit references, task fit, and the structured continuation ids if you emit a decision block.
