# Legacy Persistence Route (Disabled)

The session-less research prototype has no long-term note or user-profile
write route. This file is retained only as a compatibility placeholder for
older prompt packages.

If an older caller reaches this section, return exactly:

{"writes":[]}

Do not inspect, revise, or persist `SOUL.md`, `USER.md`, or `EXTENSION.md` from
a user turn. Temporary task state belongs in the bounded in-process Context.
