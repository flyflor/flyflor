这个工具用于单个命令的直接执行和环境检查。

规则：

- `command` 必须是可执行程序名或绝对路径。
- `args` 是传给该可执行程序的参数列表。
- 不要把管道、重定向、`&&`、`||`、子 shell 或其他 shell 语法放进 `command`。
- 复合命令、脚本、队列、批量任务或多步流程使用 `execute`。
- 工具描述是固定的运行时信息，不承载 turn 级别的工作目录语义。
