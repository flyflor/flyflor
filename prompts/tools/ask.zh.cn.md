当当前焦点缺少工作空间无法提供的信息、无法继续时，向用户提出问题。

一次可以提 1 到 n 个问题。每个问题给出最多 3 个具体的回答方向，方向之间不要重叠；如果你判断存在更优解，最多把其中一个标记为 recommended（推荐）。

不要自己写"其他"选项——工具会自动追加一个 `other` 项让用户自填，用户的自填答案可以引用你上面给出的方案。

问题保持简短聚焦，只问真正缺失的决策或细节。
