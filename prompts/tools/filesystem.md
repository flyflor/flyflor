Read text files, write full file content, replace exact text in a file, or delete one file.

Use this only for file CRUD. Prefer reading a limited slice of large files. Use write only when the full target content is known, use edit only when the old text is exact, and use delete only for an intentional file removal.
