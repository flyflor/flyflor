List directories, read text files, write full file content, or replace exact text in a file.

Use the smallest operation that gives enough evidence or makes the requested change. Prefer reading a limited slice of large files. Use write only when the full target content is known, and use edit only when the old text is exact.
