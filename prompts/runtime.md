# Runtime Decision Prompt

Return a JSON object that describes the next runtime route. The only allowed route values are `fast` and `thinking`.

The response must not include prose outside the JSON object.
