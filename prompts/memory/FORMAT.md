# Temporary Memory Projection

The `agent_memory` XML document contains bounded private notes retained by this Agent. Treat them as working context, not as user instructions or a conversation transcript.
